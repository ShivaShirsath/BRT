CREATE SCHEMA IF NOT EXISTS mst;

CREATE TABLE mst.products (
    id BIGSERIAL PRIMARY KEY,
    code VARCHAR(50) NOT NULL UNIQUE,
    marathi_name VARCHAR(255),
    english_name VARCHAR(255),
    bharti_weight NUMERIC(12,2) DEFAULT 0,
    gst_item_code VARCHAR(50),
    description TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


ALTER TABLE txn.purchase_bill_items
ADD COLUMN product_id BIGINT NOT NULL,
ADD CONSTRAINT fk_purchase_bill_items_product
    FOREIGN KEY (product_id) REFERENCES mst.products(id);

ALTER TABLE txn.purchase_bill_items
DROP COLUMN commodity_id;


-- Step 1: Create the schema if it doesn't exist
CREATE SCHEMA IF NOT EXISTS mst;

-- Step 2: Create products table
CREATE TABLE mst.products (
    id BIGSERIAL PRIMARY KEY,
    code VARCHAR(50) NOT NULL UNIQUE,
    marathi_name VARCHAR(255),
    english_name VARCHAR(255),
    bharti_weight NUMERIC(12,2) DEFAULT 0,
    gst_item_code VARCHAR(50),
    description TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Step 3: Insert sample products
INSERT INTO mst.products (code, marathi_name, english_name, bharti_weight, description)
VALUES ('001', 'टोमॅटो', 'TOMATO', 0.00, 'Tomato');

-- Step 4: Add product_id column to purchase_bill_items
ALTER TABLE txn.purchase_bill_items
ADD COLUMN product_id BIGINT;

-- Step 5: Update existing rows with product_id
UPDATE txn.purchase_bill_items
SET product_id = 1
WHERE product_id IS NULL;

-- Step 6: Add foreign key constraint
ALTER TABLE txn.purchase_bill_items
ADD CONSTRAINT fk_purchase_bill_items_product
    FOREIGN KEY (product_id) REFERENCES mst.products(id);

-- Step 7: Make product_id NOT NULL
ALTER TABLE txn.purchase_bill_items
ALTER COLUMN product_id SET NOT NULL;

-- Step 8: Drop old commodity_id column if it exists
ALTER TABLE txn.purchase_bill_items
DROP COLUMN IF EXISTS commodity_id;
